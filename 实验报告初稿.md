# 高级语言程序设计大作业实验报告

姓名：耿浩喻

学号：2512346

班级：模拟1-1

日期：2026年5月2日

## 一、作业题目

Pulse Harbor：基于 C++ 与 Win32 图形界面的陨石生存躲避小游戏

## 二、开发软件

- 编程语言：C++
- 开发环境：Windows
- 编译器：g++ 14.2.0（MSYS2 UCRT64）
- 图形化平台：Win32 API + GDI
- 版本管理平台：Gitee 或 GitHub

## 三、课题要求

本项目按照课程大作业要求，使用 C++ 完成一个图形化小程序。程序采用原生 Windows 图形界面，不依赖第三方游戏引擎或图形库，能够独立运行。项目内容为一款带有实时交互、碰撞检测、状态切换和分数统计功能的小游戏，兼顾完整性、可玩性与一定的创意性。

本项目的主要目标如下：

- 设计并实现一个可以实际游玩的图形化小游戏。
- 使用面向对象思想组织核心逻辑。
- 实现开始界面、游戏进行、暂停界面、结束界面等完整流程。
- 实现碰撞检测、难度变化、计分机制等核心功能。
- 为后续项目展示视频和实验报告提供较清晰的技术路线。

## 四、主要流程

### 1. 项目整体思路

本项目实现的是一款“生存躲避”类小游戏。玩家控制一个飞船在窗口中移动，躲避不断靠近的陨石。玩家还可以消耗能量释放脉冲技能，清除周围一定范围内的陨石。被清除的陨石有概率掉落绿色能量碎片，玩家拾取后可以补充能量，从而形成“躲避危险、积攒能量、释放技能、继续生存”的循环。

游戏的核心设计思想是：在简单操作基础上加入资源管理机制。若只是单纯躲避，玩法会较单一；加入能量条与技能冷却后，玩家需要考虑什么时候使用技能，从而提升策略性与可玩性。

### 2. 程序结构设计

程序采用面向对象的方式组织，核心类为 `Game` 类。该类负责统一管理游戏状态、输入处理、实体更新、碰撞检测、界面绘制和场景切换。

主要数据结构如下：

- `Vec2`：二维向量结构，用于表示位置、速度等信息。
- `Player`：保存玩家位置、生命值、速度、能量和无敌时间等状态。
- `Meteor`：保存陨石的位置、速度、半径和运动扰动参数。
- `EnergyShard`：表示能量碎片，用于能量恢复与得分奖励。
- `PulseRing`：表示释放脉冲技能时的环形特效。
- `Star`：用于背景星点绘制，提升界面表现力。
- `Scene`：枚举类型，用于表示菜单、游戏中、暂停、结束等不同状态。

程序入口为 `WinMain`，通过创建窗口、注册消息回调函数、启动消息循环，使整个游戏在 Windows 桌面环境下运行。

### 3. 游戏流程

游戏运行流程如下：

1. 程序启动后进入主菜单界面。
2. 玩家按下 `Enter` 键后开始游戏。
3. 游戏中通过 `WASD` 或方向键控制角色移动。
4. 系统持续生成陨石，并依据时间动态提高威胁等级。
5. 玩家碰到陨石会失去生命值，并获得短暂无敌时间。
6. 玩家按下 `Space` 可释放脉冲，消耗能量并清除周围陨石。
7. 消灭陨石后有概率产生能量碎片，拾取后恢复能量。
8. 当生命值降为 0 时，进入结算界面。
9. 玩家可按 `R` 或 `Enter` 重新开始游戏。

### 4. 核心算法与实现

#### （1）游戏循环

本项目采用基于 Windows 定时器的刷新机制，通过 `SetTimer` 每约 16ms 触发一次更新，形成接近 60 帧的运行效果。每次刷新时，程序会计算与上一帧之间的时间间隔 `dt`，再根据 `dt` 更新玩家、陨石、碎片和特效等对象状态。

这种做法的优点是结构清晰，适合课程项目实现，同时能够保证游戏速度与机器性能之间相对独立，避免不同设备运行速度差异过大。

#### （2）输入处理

程序通过 `WM_KEYDOWN` 和 `WM_KEYUP` 消息记录按键状态，实现连续移动和技能触发。相比一次按键只响应一次的写法，记录按键状态更适合实时交互类游戏。

例如：

- `WASD` / 方向键：控制移动方向。
- `Shift`：降低移动速度，便于精细操作。
- `Space`：释放脉冲技能。
- `Esc` / `P`：暂停与继续游戏。

#### （3）碰撞检测

由于游戏中的主要对象都可近似看作圆形，因此采用圆形碰撞检测。设两个圆心分别为 `A` 和 `B`，半径分别为 `r1` 和 `r2`，若两圆心距离小于等于半径之和，则视为发生碰撞。

其判断方式为：

`(A.x - B.x)^2 + (A.y - B.y)^2 <= (r1 + r2)^2`

该方法计算量小，适合实时游戏场景。

#### （4）难度动态提升

为了避免游戏一开始过难或后期过于单调，程序中将威胁等级 `difficulty` 与生存时间关联。随着时间增加：

- 陨石生成间隔逐渐缩短。
- 陨石半径上限逐渐提高。
- 陨石速度整体增加。

这种方式能够形成较自然的难度曲线，使玩家在前期适应操作，在后期体验更强的挑战。

#### （5）能量与技能机制

玩家拥有能量条，能量会随时间缓慢恢复，也可以通过拾取能量碎片加快恢复。当能量足够且技能不在冷却中时，玩家可以释放脉冲技能。

脉冲技能的作用包括：

- 清除一定范围内的陨石。
- 形成环形视觉特效。
- 提升游戏节奏感和操作反馈。

技能加入后，游戏不再只是单纯“躲避”，而是包含了“资源积累与释放时机判断”的策略元素。

#### （6）双缓冲绘制

为减少窗口绘制闪烁，程序在绘图时先创建内存设备上下文，将背景、角色、陨石、UI 等内容绘制到内存位图，再一次性拷贝到窗口。这种方式能够显著提升画面稳定性，使运行效果更平滑。

### 5. 图形界面设计

虽然本项目未使用外部美术资源，但仍通过颜色和布局设计增强界面表现：

- 深色背景配合动态星点，营造太空环境氛围。
- 玩家采用亮蓝色圆形与外圈描边，便于在场景中识别。
- 陨石使用橙黄色配色，与玩家形成明显对比。
- 绿色能量碎片用于提示可收集资源。
- 顶部区域显示得分、生存时间、威胁等级、能量条和技能冷却条。
- 中央弹窗用于显示开始、暂停和结束界面信息。

这种设计虽然实现方式较基础，但能够保证界面清晰、功能完整，并具有一定视觉效果。

## 五、单元测试与功能测试

由于本项目属于图形化交互程序，测试主要分为功能测试和逻辑验证两部分。

### 1. 功能测试内容

| 测试编号 | 测试内容 | 预期结果 |
| --- | --- | --- |
| 1 | 启动程序 | 正常弹出窗口并显示主菜单 |
| 2 | 按 `Enter` 开始 | 成功进入游戏界面 |
| 3 | 按 `WASD` / 方向键 | 玩家能够正常移动 |
| 4 | 按 `Shift` | 玩家移动速度降低 |
| 5 | 按 `Space` | 在能量充足时释放脉冲并进入冷却 |
| 6 | 玩家接触陨石 | 生命值减少，角色进入短暂无敌状态 |
| 7 | 收集能量碎片 | 能量值上升，得分增加 |
| 8 | 生命值归零 | 进入结束界面并显示最终分数 |
| 9 | 按 `R` 或 `Enter` | 从结束界面重新开始游戏 |
| 10 | 按 `Esc` 或 `P` | 游戏可暂停并继续 |

### 2. 测试结果分析

在当前版本中，以上功能均已完成并可运行。编译后能够成功生成可执行文件，程序可在本地正常打开并游玩。测试过程中重点关注了以下几个问题：

- 玩家是否会移出窗口边界。
- 技能是否会在能量不足时误触发。
- 陨石与玩家碰撞后是否正确扣血。
- 暂停后时间和实体运动是否被正确停止。
- 结束界面是否能够正常重开游戏。

经过多次运行测试，上述逻辑均能正常工作。

### 3. 可进一步完善的测试

如果后续继续完善项目，还可以补充以下内容：

- 统计不同时间段的平均帧率。
- 增加固定随机种子的自动化逻辑测试。
- 对碰撞边界值进行更细致验证。
- 对长时间运行下的内存和稳定性进行测试。

## 六、项目特色与创新点

相较于普通的“鼠标点击”或“简单绘图”类图形程序，本项目的创新点主要体现在以下几个方面：

- 不是静态演示程序，而是具有完整交互流程的实时小游戏。
- 在生存躲避基础上加入了能量资源管理与范围技能机制。
- 采用动态难度提升，使游戏过程具有明显节奏变化。
- 使用纯 Win32 API 与 GDI 完成界面渲染，减少第三方依赖，便于理解底层图形化程序结构。
- 程序包含菜单、暂停、结算、重开、记分等较完整的产品化元素，更适合作为课程展示作品。

## 七、收获与体会

通过完成本项目，我对 C++ 图形化程序开发和游戏逻辑组织有了更直观的理解。

首先，我进一步熟悉了 Windows 消息机制。与控制台程序不同，图形界面程序需要围绕消息循环组织输入与界面刷新，这对程序结构设计提出了更高要求。

其次，我体会到了面向对象设计在中型程序中的作用。若将所有功能都写在主函数中，代码会非常混乱；而通过 `Game` 类集中管理状态，再将玩家、陨石、碎片等对象分别建模，程序结构会更加清晰，也更便于后续扩展。

再次，我学习了图形化程序中的基础实时算法，包括向量计算、碰撞检测、定时刷新、双缓冲绘图等。这些内容虽然公式不复杂，但在实际项目中组合起来后，能够构成一个完整可玩的系统。

最后，我认识到一个课程项目除了“能运行”之外，还应关注可展示性、可解释性和完成度。因此在实现过程中，我不仅完成了基本功能，也尽量补充了开始界面、暂停界面、得分系统、技能机制和视觉效果，使整个项目更适合作为大作业展示。

## 八、可改进方向

本项目目前已能够满足大作业的基本要求，但仍有进一步优化空间：

- 增加音效和背景音乐，提升沉浸感。
- 增加排行榜或本地最高分保存功能。
- 引入更多敌人类型或关卡设计。
- 增加粒子特效，使技能释放更有表现力。
- 提供难度选择或双人模式等扩展玩法。

## 九、AI 工具使用披露

本项目在开发过程中使用了 AI 工具辅助完成部分工作。具体情况如下：

- AI 工具名称及版本：OpenAI Codex（GPT-5 系列编码助手）
- 具体用途：辅助进行项目方案整理、代码开发、注释与文档撰写
- 使用位置：小游戏主程序的部分实现思路、实验报告初稿撰写、项目说明文档整理

项目的需求理解、功能取舍、运行测试与最终提交内容均由本人确认和整理。

## 十、附录

### 1. 项目运行说明

- 编译命令：

```powershell
powershell -ExecutionPolicy Bypass -File .\build.ps1
```

- 可执行文件路径：

```text
build/PulseHarbor.exe
```

### 2. 项目仓库与视频链接

- 项目仓库地址：https://github.com/PerFlam/Pulse-Harbor
- B站讲解视频地址：________

